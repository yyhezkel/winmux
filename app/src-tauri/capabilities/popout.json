{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popout",
  "description": "Terminal pop-out windows (popout-*): PTY I/O mirror over app-wide events + self-close on session exit. Minimum surface; does not touch the main window.",
  "windows": ["popout-*"],
  "permissions": [
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-set-title",
    "core:window:allow-set-focus"
  ]
}
