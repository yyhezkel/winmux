# v0.4.4-beta.1 smoke test

Legend: ✅ verified · ☐ to verify live · ⚙️ covered by automated tests only.

## Automated (green at cut time)
- ⚙️ `cargo test --workspace` → pass (desktop 81 lib + settings 27 + cli 5).
- ⚙️ `go test ./...` (winmux-server) → all packages ok.
- ⚙️ `tsc --noEmit` → clean.
- ⚙️ `node --test src/textDirection.test.ts` → 23/23 (RTL detection).
- ⚙️ username scrub → no `mlastudent371` in tracked non-binary files.

## Desktop UX (verify live)
- ☐ **Auto-connect panels:** disconnected SSH workspace → click Files / Monitor
  / Browser / Ports → "מחבר…" pill → panel loads (no "no active SSH session").
  Password-auth workspace → silent no-op + existing hint.
- ☐ **New-connection picker:** disconnected pane → Connect ▾ → "חיבור חדש…" →
  pick dir + claude command → connects with both. Existing tmux session → direct
  attach, no picker.
- ☐ **Feed grouping:** permission/notification cards show workspace **name** +
  session badge; per-workspace collapsible groups; "current workspace only"
  filter works.
- ☐ **i18n feed cards:** he UI shows `הרצת כלי` / `אושר` / `נדחה` (not raw
  `pre-tool-use` / `allowed` / `denied`).
- ☐ **RTL per-line:** he mode — mixed line `שרת רץ על port 4200` renders RTL with
  "port 4200" LTR inside; pure ASCII path renders LTR; toggle Settings →
  Terminal → "כיוון אוטומטי לכל שורה" off/on works live. (Approach C — see
  `docs/RTL-TEST.md`.)
- ☐ **PARKED "RTL caret":** on a mixed RTL line, Left/Right arrows + caret now
  match the visual direction? If yes → mark PARKED (2026-06-26) resolved.

## Hook notifications (needs remote CLI redeploy to exercise)
> The hook-log + notification changes live in the remote CLI (`winmux-linux-x64`,
> rebaked at 0.4.4). Redeploy the CLI to the remote first.
- ☐ **hook-debug.log:** a normal Claude tool call writes 2 lines (`REQ…`/`ACK…`),
  not 4. `WINMUX_HOOK_VERBOSE=1` restores the full per-branch trace.
- ☐ **SessionStart / Notification:** no feed card, no log line (silent-ack).
- ☐ **Stop:** with winmux **backgrounded** → toast "🎯 Claude finished — your
  turn"; with winmux focused → no toast (feed card only).
- ☐ **SessionEnd:** toast "🎯 Session ended · <duration>".

## Release integrity
- ☐ Settings → Updates shows **v0.4.4**; app title/about reflects 0.4.4.
- ☐ MSI + NSIS bundles install cleanly; `winmux-linux-x64` embedded SHA matches
  `remote-manifest.json`.
- ☐ `manifest.json` still on the stable version (beta not auto-pushed).
